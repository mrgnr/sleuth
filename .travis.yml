language: python
python:
    - "3.2"
    - "3.3"
    - "3.4"
    - "pypy3"
install:
    - pip install -r requirements.txt
    - pip install coveralls
script:
    - nosetests --with-coverage --cover-package=sleuth
after_success: coveralls
